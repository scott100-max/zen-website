name = "email-cmd"
main = "src/worker.js"
compatibility_date = "2024-01-01"

[[r2_buckets]]
binding = "BUCKET"
bucket_name = "salus-mind"

[vars]
AUTH_TOKEN = "salus-vault-2026"
ALLOWED_SENDER = "scottripley@icloud.com"

# Secrets (set via `wrangler secret put`):
#   WEBHOOK_SECRET — Svix signing secret from Resend dashboard

[[routes]]
pattern = "cmd.salus-mind.com"
custom_domain = true
